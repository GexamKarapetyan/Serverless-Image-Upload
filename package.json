{
    "name": "image-upload-api",
    "version": "1.0.0",
    "description": "Serverless image upload + async processing API",
    "scripts": {
        "test": "jest --coverage"
    },
    "jest": {
        "silent": true
    },
    "dependencies": {
        "@aws-sdk/client-dynamodb": "^3.0.0",
        "@aws-sdk/client-s3": "^3.0.0",
        "@aws-sdk/s3-request-presigner": "^3.0.0",
        "uuid": "^9.0.0"
    },
    "devDependencies": {
        "aws-sdk-client-mock": "^4.1.0",
        "jest": "^30.3.0"
    }
}
